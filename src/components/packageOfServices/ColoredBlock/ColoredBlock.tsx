import React, { FC } from "react";
import styles from "./ColoredBlock.module.scss";

const ColoredBlock: FC = () => {
  return <div className={styles["colored-block"]}></div>;
};

export default ColoredBlock;
